# Core
python-dotenv
streamlit>=1.35.0
pydantic>=2.0.0
loguru

# PDF / image extraction
pdfplumber>=0.10.0
pdf2image
paddleocr
paddlepaddle
Pillow

# Excel / Word
openpyxl
pandas
mammoth

# NLP + Embeddings
sentence-transformers
chromadb>=0.4.0
rapidfuzz

# Graph
networkx

# LLM (local, free, open-weight — no API key)
# mlx-lm: fast Metal backend, Apple Silicon only.
# llama-cpp-python: portable CPU backend (GGUF), used on Linux hosts e.g. HF Spaces.
mlx-lm; sys_platform == "darwin" and platform_machine == "arm64"
llama-cpp-python; sys_platform != "darwin" or platform_machine != "arm64"
huggingface_hub

# Export
python-docx
matplotlib

# Utilities
httpx
tqdm
numpy
